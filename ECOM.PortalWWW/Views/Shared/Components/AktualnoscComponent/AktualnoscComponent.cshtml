@model IEnumerable<Firma.Data.Data.CMS.Aktualnosc>

@{
    ViewData["Title"] = "Index";
}

<h1 class="title">Index</h1>

@*<p>
    <a class="button" asp-action="Create">Create New</a>
</p>*@


@foreach (var item in Model)
{
    <div class="row">
        <div class="col s12 m6">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title">Card Title</span>
                    <p>
                        @Html.DisplayFor(modelItem => item.LinkTytul)
                    </p>
                    <br>
                    <p>
                        @Html.DisplayFor(modelItem => item.Tytul)
                    </p>
                </div>
                <div class="card-action">
                    <a href="#">This is a link</a>
                    <a href="#">This is a link</a>
                </div>
            </div>
        </div>
    </div>

    @*<tr>
        <td>
            @Html.DisplayFor(modelItem => item.LinkTytul)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tytul)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tresc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pozycja)
        </td>
        <td>
            <a class="link" asp-action="Edit" asp-route-id="@item.IdAktualnosci">Edit</a> |
            <a class="link" asp-action="Details" asp-route-id="@item.IdAktualnosci">Details</a> |
            <a class="link" asp-action="Delete" asp-route-id="@item.IdAktualnosci">Delete</a>
        </td>
    </tr>*@
}
<style>
    .title {
        font-size: 2rem;
        font-weight: bold;
        text-align: center;
        margin-bottom: 1rem;
    }

    .button {
        display: inline-block;
        padding: 0.5rem 1rem;
        background-color: #007bff;
        color: #fff;
        text-decoration: none;
        border-radius: 0.25rem;
        transition: background-color 0.3s;
    }

        .button:hover {
            background-color: #0069d9;
        }

    .table {
        border-collapse: collapse;
        width: 100%;
        margin-bottom: 1rem;
    }

        .table th,
        .table td {
            border: 1px solid #dee2e6;
            padding: 0.5rem;
            text-align: left;
        }

        .table th {
            background-color: #f5f5f5;
            font-weight: bold;
        }

    .link {
        color: #007bff;
        text-decoration: none;
        transition: color 0.3s;
    }

        .link:hover {
            color: #0056b3;
        }
</style>